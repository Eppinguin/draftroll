name: Agent additive duplicate die id fix

on:
  push:
    branches:
      - agent/dicebox-learnings-cards

jobs:
  additive-duplicate-die-id:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dicebox-learnings-cards
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22.14.0

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.17.0 --activate

      - name: Apply duplicate die id fix
        run: |
          node --check scripts/agent-fix-additive-duplicate-die-ids.mjs
          node scripts/agent-fix-additive-duplicate-die-ids.mjs
          rm scripts/agent-fix-additive-duplicate-die-ids.mjs
          rm .github/workflows/agent-additive-duplicate-die-id-fix.yml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format changed files
        run: >-
          pnpm exec oxfmt --write
          src/physical-table.ts
          src/main.ts
          scripts/test-visual-fallbacks.mjs
          tests/browser/specs/overlay.spec.ts
          docs/ARCHITECTURE.md

      - name: Verify formatting
        run: >-
          pnpm exec oxfmt --check
          src/physical-table.ts
          src/main.ts
          scripts/test-visual-fallbacks.mjs
          tests/browser/specs/overlay.spec.ts
          docs/ARCHITECTURE.md

      - name: Lint and typecheck
        run: >-
          pnpm lint &&
          pnpm exec tsc --noEmit

      - name: Core regression
        run: pnpm test:core

      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Build browser fixtures
        run: pnpm test:browser:build

      - name: Overlay browser regression
        run: pnpm exec playwright test tests/browser/specs/overlay.spec.ts --project=chromium

      - name: Commit validated implementation
        run: |
          git status --short
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "Implementation produced no changes"
            exit 1
          fi
          git commit -m "allow duplicate die ids on additive tables"
          git push origin HEAD:agent/dicebox-learnings-cards
